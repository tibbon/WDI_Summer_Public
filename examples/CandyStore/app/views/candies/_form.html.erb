<%= form_for(@candy) do |f| %>
  <% if @candy.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@candy.errors.count, "error") %> prohibited this candy from being saved:</h2>

      <ul>
      <% @candy.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :price %><br>
    <%= f.text_field :price %>
  </div>
  <div class="field">
    <%= f.label :flavors %><br>
    <%= f.collection_check_boxes :flavors, 
        %w(sweet peanutbutter carmel), :to_s, :to_s %>
  </div>

  <div class="field">
    <%= f.label :created_at %><br>
    <%= f.date_select :created_at %>
  </div>

  <div class="field">
    <%= f.label :awards %><br>
    <%= f.text_field :awards %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
