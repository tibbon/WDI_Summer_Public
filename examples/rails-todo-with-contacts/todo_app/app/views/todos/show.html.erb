<h2>Task</h2>
<p>Name: <%= @todo.task %></p>
<p>Description: <%= @todo.description %></p>
<p>Due Date: <%= @todo.due_date %></p>
<p>Completed: <%= @todo.completed %></p>

<h3>Contacts assigned to this task:</h3>
  <% @todo.contacts.each do |contact| %>
  <p>
    <%= render partial: contact %>
    <%= button_to "Remove Contact",
      remove_todo_contact_path(contact_id: contact.id) %>
  </p>
  <% end %>

<h3>Not on this task:</h3>
  <% @available_contacts.each do |contact| %>
  <p>
    <%= render partial: contact %>
    <%= button_to "Assign Contact",
      add_todo_contact_path(contact_id: contact.id) %>
  </p>
  <% end %>



<a href="/todos/<%= @todo.id %>/edit">Edit Task</a>
<br/><br/>
<form action="/todos/<%= @todo.id %>" method="post">
  <input type="hidden" name="_method" value="delete">
  <button>Delete Task</button>
</form>